<% if @template %>

  <%= turbo_frame_tag @property do %>
    <main class="container">
    
      Many properties are similar on the inside or outside. Please choose an idea below that sounds close to what this property is. 
    
      <%= render "form", property: @property, template: "everything" %>

    </main>
  <% end %>

<% else %>

  <main class="container">
    <%= link_to sanitize("&larr; Back to property"), property_path(@property) %>
    
    <div class="header">
      <h1>Edit property</h1>
    </div>
    
    <%= turbo_frame_tag @property do %>
      <%= render "form", property: @property %>
    <% end %>
      
  </main>

<% end %>
